<% if logged_in? %>
  <h1>タスク一覧</h1>
  
  <% @tasks.each do |task| %>
  <ul>
    <li><%= link_to task.content, task %> : <%= task.status %></li>
  </ul>
  <% end %>
<% else %>
  <h1>こんにちは</h1>
<% end %>